mixin header()
  -
    const mock1 = [
      {text: "Продукты"},
      {text: "Цвета"},
      {text: "Вдохновение"},
      {text: "Советы"},
      {text: "Найти магазин"}
    ]
  -
    const mock2 = [
      {path: "search", size: 20},
      {path: "profile", size: 22},
      {path: "favorite", size: 22}
    ]
  header.header
    .header__wrap
      +btn().header__toggler
    .header__cont
      +btn({href: 'index.html'}).header__logo
        img(src="img/content/logo.svg", width="131", height="26", alt="Логотип.")
      nav.header__nav
        each item in mock1
          +btn({href: '#'}).header__link.btn!= `${item.text}`
      .header__box
        +btn({href: 'tel:+78005558628'}).header__tel.btn!= "+7 (495) 221-77-69"
        span.header__sign!= "Заказать звонок"
      .header__tools
        each item in mock2
          +btn({href: '#'}).header__tool.btn
            img(src=`img/sprite/${item.path}.svg`, width=`${item.size}`, height=`${item.size}`, alt=`${item.path}.`)
        +btn({href: '#'}).header__cart.btn
